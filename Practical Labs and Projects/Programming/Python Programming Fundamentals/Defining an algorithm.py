# Algorithm to calculate the sum of two numbers
def compute_sum(x, y):
    return x + y

# Taking user input
a = float(input("Enter first number: "))
b = float(input("Enter second number: "))

# Compute and display result
result = compute_sum(a, b)
print("The sum is:", result)
